# NestJS API base URL (no trailing slash).
# localhost is rewritten automatically on Expo Go (physical device) to your PC's LAN IP.
# Override with your machine IP if needed, e.g. EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
